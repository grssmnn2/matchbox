<!-- Modal -->
<div id="modal-{{id}}" class="modal fade" role="dialog">
<div class="modal-dialog">

    <!-- Modal content-->
    <div class="modal-content">
        <div class="modal-header">
            <h4 class="modal-title">{{name}}</h4>
            <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        <div class="modal-body">
            <img class="modal-img" src="{{image_url}}">
            <p class="dash-blurb">{{description}}</p>
            <h5>Price: ${{price}}/month</h5>
        </div>
        <div class="modal-footer">
            <div id="user_id" data-key="{{userProfile.id}}"></div>
            <div id="box_id" data-key="{{id}}"></div>
            <button id="switch" type="button" class="btn btn-default accent" data-dismiss="modal">Switch My Subscription!</button>
        </div>
    </div>

</div>
</div>